import 'package:flutter/material.dart';

const int totalPages = 10; // Total number of pages
const Duration timerDuration = Duration(minutes: 1); // Total game duration

// Text Styles
const TextStyle largeTextStyle =
    TextStyle(fontSize: 24, fontWeight: FontWeight.bold);
